import { createAction } from 'redux-actions';

export const addSmile = createAction('add smile');
export const addFrown = createAction('add frown');
export const submitFeedback = createAction('submit feedback');
